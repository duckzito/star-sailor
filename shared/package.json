{
  "name": "@star-sailor/shared",
  "version": "0.1.0",
  "private": true,
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "echo 'shared: consumed as source by client (vite) and server (esbuild)'"
  }
}
